import {Injectable} from '@angular/core';
import {Zones, Sites, PlaceMarks, Layers, Sensors} from './db';

@Injectable({
  providedIn: 'root'
})
export class MockServerService {

  constructor() {
  }
}
